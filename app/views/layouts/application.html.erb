<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
  	<meta charset="utf-8" />

  	<!-- Uncomment to make IE8 render like IE7 -->
  	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

  	<!-- Set the viewport width to device width for mobile -->
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  	<%= stylesheet_link_tag    "application" %>
  	<%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <div class="row">
      <div class="large-12 columns">
        <h1>Collab.io <small>insert a description here</small></h1>
        <div>
          <% if user_signed_in? %>
            <p> <%= image_tag current_user.image %> Hello <%= link_to(current_user.name, edit_user_registration_path) %> <%= link_to "logout", destroy_user_session_path, method: :delete, class:"small alert button", confirm:"Logout?" %></p>
          <% else %>
            <%= link_to "Sign in with facebook", user_omniauth_authorize_path(:facebook), class:"small info button" %> to get started
          <% end %>
        </div>
      </div>
    </div>

    <hr />

    <%= yield %>

    
    <%= javascript_include_tag "application" %>
  </body>
</html>
